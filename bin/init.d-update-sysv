#!/bin/bash

# just because I forgot the name

echo "run: "
#echo "  update-rc.d service start"
#echo "  update-rc.d service enable"
#echo "both needed??"

echo "  update-rc.d servicename defaults"
echo "while there are no symlinks done yet!"
echo "  update-rc.d servicename disable"
