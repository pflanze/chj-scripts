#!/bin/bash

IFS=

logdir=~/log
mkdir -p "$logdir"

did_run=0
onexit() {
    init-xscreensaver start || true
    init-xwrits-mine start || true
}

init-xscreensaver stop
init-xwrits-mine stop

trap onexit EXIT INT

"$@"
