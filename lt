#!/bin/bash

set -euo pipefail

tail=20

if [ $# == 0 ]; then
    dir=.
elif [ $# == 1 ]; then
    dir=$1
else
    echo "usage: $0 [dir]"
    echo "   list the last $tail files in the given dir"
    exit 1
fi

exec lst --ls-dir "$dir" --ignore '(^|/)\.' --ignore '~$' -lrt tail $tail
